# Compile-Time References

These DLLs are checked in so `LiveSplit.AlternativeTimer` can build without cloning sibling LiveSplit repositories.

They are compile-time references only. LiveSplit provides the runtime copies of these assemblies when the component is loaded.

Do not include this folder in the normal release package.
